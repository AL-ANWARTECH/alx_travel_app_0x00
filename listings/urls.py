from django.urls import path
from . import views

app_name = 'listings'

urlpatterns = [
    # Add your listing URLs here
    # Example patterns you might add later:
    # path('', views.listing_list, name='listing_list'),
    # path('<int:pk>/', views.listing_detail, name='listing_detail'),
]